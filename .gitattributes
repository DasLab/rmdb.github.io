# (Previously *.rdat was LFS-tracked; switched to GitHub Release assets in
# data-v1 release because GitHub Pages doesn't transparently serve LFS objects.
# Old LFS pointers remain in git history but no new .rdat files are LFS-tracked.)
